_site
.idea
.sass-cache
.jekyll-metadata
.jekyll-cache
Gemfile
Gemfile.lock
/.vscode
.playwright-mcp
.env
.worktrees
node_modules
dist
.astro
.vercel
.env*.local
.env*.local.bak
.agent.json
.env.production

# Internal working docs — kept locally, not tracked in the repo
AUDIT_LOG.md
UI_FIXES_LOG.md
DEV_DOCS_INTEGRATION.md

# Claude / local dev tools — screenshots saved during dev sessions
# (CLAUDE.md and AGENTS.md are committed — agent/contributor guides.)
scripts/screenshot.mjs
screenshot-*.png
audit-*.png
check-*.png
final-*.png
header-*.png
toc-*.png
diag-*.png
cur-*.png
fix-*.png
before-*.png
after-*.png
nav-test-*.png
pw-*.png
stripe-*.png
sidebar-scroll-test.png

# internal working files — local-only, never ship in docs PRs
apps-section/
.claude/

# local screenshot-capture session (never commit)
apps-section/auth.json
apps-section/shots/
apps-section/_shots/
apps-section/_*.mjs
