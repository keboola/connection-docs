---
/**
 * Empty override of Starlight's <LastUpdated>.
 *
 * The page date is rendered once, in the meta row at the top of the page by
 * PageTitle.astro ("Updated October 25, 2023"). Starlight would render it a
 * second time in the footer next to "Edit page"; this override removes that
 * copy. The date itself comes from frontmatter `lastUpdated`, written by
 * scripts/gen-last-updated.mjs — see its header for why it is precomputed.
 */
---
