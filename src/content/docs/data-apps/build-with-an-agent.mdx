---
title: Build an app with an AI agent
slug: 'data-apps/build-with-an-agent'
description: 'Build and deploy a Keboola app from Claude Code, Claude Desktop, Cursor, VS Code or the ChatGPT app with the dataapp-developer plugin and kbagent: set up the agent, describe the app, then check its URL, password and logs.'
---

import { Tabs, TabItem } from '@astrojs/starlight/components';

{/* Claude Code flow verified live on 2026-09-23 in project 264 (europe-west3): Claude Code 2.1.280, kbagent 0.94.0, dataapp-developer 1.6.1 from marketplace keboola-claude-kit 1.14.0. One prompt produced a running app (App ID 74021867) in about 11 minutes, including the Storage workaround below. Cursor reviewed by Michal Ševčík on 2026-09-25 (PR #1143 comment, Grok 4.7): agent mode is the default, the route question appears, a prompt that names kbagent reads better, the password is the main friction. Claude Desktop, VS Code and ChatGPT app tabs: VERIFY the dataapp-developer install and the build flow. */}

An AI agent on your computer can build a Keboola app from one prompt. With the `dataapp-developer` plugin from Keboola's [AI Kit](/ai/ai-kit/), it reads your data, writes the code, creates the app with a Git repository that Keboola manages, pushes the code there and deploys it. You end up with a running app at its own URL, and the code in a repository you can keep changing.

To build inside Keboola instead, [Kai](/data-apps/getting-started/) does the same from the **Create App** screen, with a live preview. To write the code yourself in your own repository, see [Build an app locally](/data-apps/build-locally/).

## Before you start

- A Keboola project with the data the app should show.
- [kbagent](/cli/), connected to that project. Creating the push token for the app's repository needs admin rights in the project.
- An AI client: Claude Code, Claude Desktop, Cursor, VS Code or the ChatGPT app. Using another agent? See [Other agents](#other-agents).

## What you get by default

Unless the prompt says otherwise, the agent builds a Python/JS app (for a dashboard, Node.js with Chart.js) and keeps its code in a new Keboola-managed Git repository. The app runs at the `tiny` size, sleeps after 15 minutes without visitors, reads your Storage data, and sits behind a shared password.

The agent usually can't show you that password: `kbagent data-app password` needs a Manage API token. It's on the app's page in Keboola, next to **Open App**. That page is `https://<your Keboola URL>/admin/projects/<project-id>/data-apps/<config-id>`, so ask the agent for the link.

To make the app public, say so in the prompt. Anyone with its URL can then open it and see the data it shows. Decide this when you ask for the app: a draft can't be public, and changing an existing app's sign-in, including to single sign-on, belongs in its [Authentication](/data-apps/authentication/) settings.

{/* Defaults from the kbagent create dry run (2026-09-23) and the skill's app-type choice. The app page URL pattern is the ui-detail link the Keboola MCP server returns for a data app. "A draft can't be public": Keboola rejected public access on a draft in Michal Ševčík's Cursor run (2026-09-25); asking the agent to remove the password of an existing app set auth_required to false and redeployed, yet the proxy still asked for the password 6.5 minutes later. VERIFY(Michal Ševčík) whether that change takes effect later or needs the UI. */}

## Set up your client

<Tabs syncKey="ai-client">
  <TabItem label="Claude Code">
    1. Install kbagent, connect your project and add the `kbagent` plugin. [kbagent with AI agents](/cli/for-agents/#claude-code) has the steps; `/kbagent:setup` with your stack's URL installs the CLI and signs you in.
    2. Add the app-building plugin from the same marketplace:

       ```
       /plugin install dataapp-developer@keboola-claude-kit
       ```

    3. Paste the prompt from [Describe the app](#describe-the-app). Approve the commands it asks to run, unless you've allowed them.

    The plugin gives the agent a skill with Keboola's app layout, Storage access and deployment rules, plus app templates.

    {/* plugin.json 1.6.1 declares the plugin's server as type "sse" at https://mcp.us-east4.gcp.keboola.com/mcp, which answers HTTP 405 in Claude Code (`claude mcp list`, 2026-09-23). Claude Code hides it when a claude.ai connector has the same URL. */}
  </TabItem>
  <TabItem label="Claude Desktop">
    {/* VERIFY(Michal Ševčík): not yet run in the Desktop chat (the 2026-09-23 "Desktop" run was a Claude Code session in the Code tab). Check: (1) the name of the dataapp-developer card, (2) whether the agent can run kbagent here or goes MCP-only, as the skill's Path A says, (3) the prompt below, end to end. The Keboola MCP connector itself was confirmed on 2026-09-23 (project 264). */}
    1. Connect Keboola's MCP server for your stack, as in [Using with Claude Desktop](/ai/mcp-server/#using-with-claude-desktop).
    2. Open **Customise → Plugins → Add → Add from marketplace**, paste `keboola/ai-kit`, and add `dataapp-developer`. The same route installs the `kbagent` plugin; see [kbagent with AI agents](/cli/for-agents/#claude-desktop).
    3. Start a new chat and paste the prompt from [Describe the app](#describe-the-app). The skill prefers the MCP route in Claude Desktop, described in [The MCP route](#the-mcp-route); if it also finds kbagent, it asks which one to use. Without a way to push to Git, it builds a Streamlit app instead.
  </TabItem>
  <TabItem label="Cursor">
    {/* Reviewed by Michal Ševčík, 2026-09-25: the route question appears (kbagent, the plugin's MCP server, the user's MCP server) and agent mode is Cursor's default. VERIFY(Michal Ševčík): dataapp-developer under Keboola Ai Kit, and a run that stays on kbagent end to end (his run went through the plugin's MCP server into a project on us-east4). */}
    1. Install kbagent and connect your project in a terminal, as in [First, in a terminal](/cli/for-agents/#first-in-a-terminal).
    2. Add the AI Kit marketplace as in [Cursor](/cli/for-agents/#cursor), with the full URL `https://github.com/keboola/ai-kit`. Under **Keboola Ai Kit**, add both `kbagent` and `dataapp-developer`.
    3. Paste the prompt from [Describe the app](#describe-the-app) into Cursor's chat. Approve the terminal commands it asks to run.
  </TabItem>
  <TabItem label="VS Code">
    {/* VERIFY(Michal Ševčík): install route from cli/for-agents#vs-code, tested 2026-08-26 for kbagent only. Check: (1) whether the picker offers dataapp-developer and takes one plugin per run, (2) whether agent mode has to be switched on and how the approval prompt for terminal commands looks, (3) which Copilot plan the flow needs, (4) the prompt below, end to end. */}
    VS Code runs the agent through GitHub Copilot, so you need the Copilot extension with agent mode.

    1. Install kbagent and connect your project in a terminal, as in [First, in a terminal](/cli/for-agents/#first-in-a-terminal).
    2. Install the plugins from source as in [VS Code](/cli/for-agents/#vs-code): run **Chat: Install Plugin from Source**, paste `https://github.com/keboola/ai-kit`, confirm the Trust prompt, and pick `kbagent`. Do the same for `dataapp-developer`.
    3. Open the Chat view (`⌃⌘I`, or `Ctrl+Alt+I` on Windows), switch to agent mode, and paste the prompt from [Describe the app](#describe-the-app). Approve the terminal commands it asks to run.
  </TabItem>
  <TabItem label="ChatGPT app">
    {/* VERIFY(Michal Ševčík): install route from cli/for-agents#chatgpt-app, tested 2026-08-26 for kbagent only. Check: (1) the app's name and version (ChatGPT or Codex), (2) the Personal tab lists dataapp-developer, (3) the prompt below, end to end. `codex plugin add` needs a current Codex CLI; the one bundled in Codex.app 0.131.0-alpha.9 has only `codex plugin marketplace`. */}
    1. Install kbagent and connect your project in a terminal, as in [First, in a terminal](/cli/for-agents/#first-in-a-terminal).
    2. Turn on **Developer mode** and add the marketplace as in [ChatGPT app](/cli/for-agents/#chatgpt-app), then install `kbagent` and `dataapp-developer` from the **Personal** tab. From a shell, that's:

       ```bash
       codex plugin marketplace add https://github.com/keboola/ai-kit
       codex plugin add kbagent@keboola-claude-kit
       codex plugin add dataapp-developer@keboola-claude-kit
       ```

    3. Start a new chat and paste the prompt from [Describe the app](#describe-the-app). Approve the commands it asks to run.
  </TabItem>
  <TabItem label="Terminal">
    Without an agent, run the same steps yourself.

    1. [Install kbagent](/cli/getting-started/) and [connect your project](/cli/getting-started/#step-2--connect-your-project).
    2. Create the app, push your code to its repository and deploy it:

       ```bash
       kbagent data-app create --project <alias> --name "Orders per day" --slug orders-per-day --use-managed-git-repo
       kbagent data-app git-repo --project <alias> --app-id <id>
       kbagent data-app git-credentials-create --project <alias> --app-id <id> --type http_token --permissions readWrite --yes
       git push <https-url> main
       kbagent data-app deploy --project <alias> --app-id <id> --wait
       ```

    `<alias>` is your project's name in kbagent; `kbagent project list` shows it in the **Alias** column. `create` prints the app ID and the config ID, and doesn't deploy, because the new repository is still empty. `git-repo` prints the repository's HTTPS URL. The push token is shown only once; when Git asks, use any username and the token as the password. The code has to follow the layout in the skill's [reference](https://github.com/keboola/ai-kit/blob/main/plugins/dataapp-developer/skills/dataapp-development/references/python-js-apps.md).
  </TabItem>
</Tabs>

## Describe the app

Say what the app shows, which data it uses and where the code goes, and ask for a new, deployed app. Without the word *new*, the skill prefers changing an app that already exists. For example:

```text
Build a Keboola app using kbagent that shows the number of orders per day as a
line chart, from the orders table in my project. Put the code in a new
Keboola-managed Git repository, deploy the app, and give me its URL and the
link to its page in Keboola.
```

The agent reads the skill, finds the table and queries a sample before it writes any code. Name a framework if it matters to you. To use your own GitHub repository instead, put its URL in the prompt; the repository then has to follow the layout in [Build an app locally](/data-apps/build-locally/).

If the agent can reach Keboola more than one way, it asks which to use: kbagent, the plugin's own MCP server, or an MCP server you connected. Pick kbagent to follow this page, or see [The MCP route](#the-mcp-route). The plugin's own server is fixed to the US GCP stack (`us-east4`). Claude Code can't connect to it and Cursor offers to sign you in, so skip it unless your project is on that stack; otherwise the app can end up in a project there instead of yours. Either way, the agent pushes the code with Git, so Git has to be installed.

{/* The route question: Claude Code, 2026-09-23 ("Keboola MCP (Recommended)" or "kbagent CLI", project 264), and Cursor, 2026-09-25 (Michal Ševčík: kbagent on project 5934, plugin-dataapp-developer-keboola MCP, user-keboola MCP). With "using kbagent" in the prompt, his agent still signed in to the plugin's MCP server and built a draft in project 133 on us-east4, so naming kbagent in the prompt doesn't pin the route. MCP route in Claude Code: prod app 74021869 (managed repo, parameters.dataApp.git written, not deployed until approved) and draft 74021870 (dev mode, branch orders-per-day, git user kai), which got its workspace. */}

### The MCP route

Through a Keboola MCP server, the agent doesn't run kbagent. It calls the server's app tools instead, such as `modify_python_js_data_app`, `create_python_js_data_app_git_credential` and `deploy_data_app`:

- It creates the app with a Keboola-managed repository, and a draft of it next to the production app.
- The draft runs in development mode at its own URL, so you preview the app before anything goes live. If the repository has a `keboola-config/supervisord-dev/` program, the draft reloads each push within seconds; otherwise the agent redeploys it.
- The production app stays undeployed until you approve the draft. Then the agent merges the draft into production and deploys it.
- The repository block is in the configuration from the start, so [the workspace bug](#if-the-app-cant-read-storage) doesn't apply.

## Check the app

- The agent finishes with the app's URL and, if you asked, the link to its page in Keboola.
- The app asks for its password, which is on that page next to **Open App**.
- If the app opens without data, ask the agent to read the app's log. [Troubleshooting](/data-apps/troubleshooting/) lists the common causes, including `Promise.withResolvers is not a function` from a too-new `@keboola/api-client`.

### If the app can't read Storage

Until [keboola/cli#765](https://github.com/keboola/cli/pull/765) ships (still the case in kbagent 0.95.0), an app created with `--use-managed-git-repo` gets no workspace, however often you redeploy it with kbagent. It runs, but its code finds no `WORKSPACE_ID`. An app built from the plugin's Node.js template logs `Missing env vars: WORKSPACE_ID (or KBC_WORKSPACE_MANIFEST_PATH)`.

The app's configuration is missing its repository block. Ask the agent to add it and deploy again, or run it yourself:

```bash
kbagent config update --project <alias> --component-id keboola.data-apps --config-id <config-id> --merge --configuration '{"parameters":{"dataApp":{"git":{"repository":"<https-url>","branch":"main","private":true}}}}'
kbagent data-app deploy --project <alias> --app-id <id> --wait
```

If you no longer have the config ID from `create`, `kbagent --json data-app detail --project <alias> --app-id <id>` shows it as `config_id`. After the fix ships, `deploy` adds the block itself.

{/* Reproduced 2026-09-23 on App ID 74021867: deploys ran without WORKSPACE_ID; config update (version 5) plus deploy loaded the data. Tracked as CLI-15. keboola/cli#765 still open at the v0.95.0 release (2026-09-24). Remove this section once it is released, and the matching troubleshooting row. */}

## Change the app later

Ask the agent for the change. It pushes to the same repository and deploys again, and the app restarts on each deploy. To edit the code yourself, clone the repository from `kbagent data-app git-repo` with a fresh token from `git-credentials-create`.

Stopping and waking the app, secrets, settings and deleting are in [Operate and update an app](/data-apps/operate/). kbagent has no commands for drafts or copying an app, and `kbagent data-app deploy --config-version` runs an older configuration for one deploy without restoring it. Drafts, copying and a real [rollback](/data-apps/operate/#see-what-changed-and-go-back) happen in the Keboola UI.

## Other agents

Any agent that can run shell commands can drive kbagent with the commands in the **Terminal** tab. For the push, it has to hand Git the token without a prompt, for example through a credential helper, because an agent's shell can't answer one. Have it load kbagent's full command reference with `kbagent context` first ([the context reference](/cli/for-agents/#the-context-reference)). If the agent can't install plugins, give it the skill as files: the folder is [on GitHub](https://github.com/keboola/ai-kit/tree/main/plugins/dataapp-developer/skills/dataapp-development), with the app templates and reference guides the skill points to.

{/* Claude Code, 2026-09-23: the kbagent route ran end to end (App ID 74021867); the MCP route ran up to the draft preview (prod 74021869 left undeployed, draft 74021870 reloaded a push and loaded 36,043 orders). The merge, prod deploy and draft cleanup (TOOLS.md, modify_python_js_data_app scenario A) were not run. The other tabs are pending review (VERIFY). */}

---

**Next:** [Operate and update an app →](/data-apps/operate/)
