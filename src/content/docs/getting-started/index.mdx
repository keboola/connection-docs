---
title: 'Getting started with Keboola'
slug: 'getting-started'
description: 'Build one working, scheduled pipeline: load six tables, join them with SQL, ask the result a question, turn it into an app, and have it rebuild itself every morning.'
redirect_from:
  - /tutorial/
---

import PageMeta from '../../../components/getting-started/PageMeta.astro';
import Prereqs from '../../../components/getting-started/Prereqs.astro';

<PageMeta genre="Tutorial" minutes="80" />

Your data lives in more places than one: files, a spreadsheet someone keeps, an API, a database.
Keboola is where you bring it together, shape it with SQL, ask it questions in plain language, turn
the answer into an app, and have the pipeline rerun by itself every morning. This guide does each
of those once, on a small sample, so you can do them on your own data.

<video autoplay loop muted playsinline poster="/getting-started/hub-demo-poster.png" aria-label="Two questions typed into Kai and answered from the staffing table, the paragraph that describes an app, the project's list of apps, and the finished grid of six cafés by seven days with the two short-handed cells outlined, then a closing line pointing at the seven steps below">
  <source src="/getting-started/hub-demo.mp4" type="video/mp4" />
</video>

{/* Reordered 2026-09-23 (Nikita): the hub opens the way a course does. A "why" paragraph, the clip,
the state block every task page already carries, the sample with the question as the course's goal,
then the table with its column renamed from "What happens" to "What you'll be able to do". Before
this the question was the first line and the reader had 74 words of context before the table; the
guide test and the Coursera / DeepLearning.AI openings both put outcomes before the puzzle. The clip
is hub-demo since commit 2; two of Jordan's 21 Aug decisions (three acts; question frame as
poster) changed with it, VERIFY(Jordan). DECISIONS.md
2026-09-23.

Tutorial-hub page: the arc index. "Get a project" moved back to its own page 2026-09-14 (see
that page's comment and DECISIONS.md), and the café story that used to open this page is gone:
Michal, 2026-09-02, "dataset je jen helping vehicle… není to nosné téma… já bych v tom byl sušší".
The world is now described once, on load/. Timing: 80 = this page (5) + the seven steps
(5+15+15+5+15+15+5); each step = reading one tab at 200 words/min plus the wall time Kai took in the
2026-09-08/09 live runs. Building alone, prompt path, was about 30 minutes.

The tab-sync GIF follows the pattern the component docs already use for UI behaviour
(public/extend/component/ui-options/ui-examples/*.gif): cropped to just the region that carries the
point, a handful of states rather than smooth motion, and small. Built 2026-09-22 from the local
preview of this branch, so it needs no project and shows no project data. Five states captured at
1000x384 (the sidebar's current-page highlight on the left, the tab strip on the right), held by
repeating frames at 5 fps, then palettegen at 96 colours and `gifsicle -O3 --lossy=40`, which
collapses the repeats back to five real frames: 104 KB, 7.2 s. The two big GIFs in this repo
(kai/Capabilities.gif at 2.5 MB, ai/mcp-server's at 9.5 MB) are the pattern NOT to copy on a page
this size.

Hero clip hub-demo, bright since 2026-09-25 (first cut 2026-09-23, replacing outlook-tour). Nikita
found the 09-23 cut dark, and it was: a scrim dimmed everything around each highlight, so the clip
averaged a luma of 71 of 255 against 229 for its screenshots. It is now five compositor scenes in
the `bright` look (no scrim; a blue outline marks the point; the camera eases in on the what-if and
the Apps list only, because on the other beats it pushed content under the caption or cut the page
title), joined with 0.35 s crossfades: 24.3 s, 671 KB, luma mean 223 and never under 221. Beats: Kai
answering "which café is short-handed" (headline outlined, the flagged rows clear of the caption),
Kai answering the what-if "one extra person" (answer and table), the Create App screen with the
guide's own prompt in the field and not sent, the project's Apps list cut to the Name column, and
the draft-preview grid (app/grid-preview.png, 14 Sep) with the two red Brno cells outlined, then
the closing line with the outline gone. Captions are 22 px on a 1280 canvas, 13.75 px at the
page's ~800 px width. Everything shot in 264 on 23 Sep at 2x, cropped below the top bar and away
from the chat history and the Last Change column, so no name, avatar or e-mail is in frame; the two
Kai questions are read-only and the prompt was never sent, so the shoot created nothing. Kai's
dates and figures are that day's; the page says the answer moves. Poster = the grid with the red
cells outlined and its caption, the answer to the question. Known and not fixed from the 14 Sep
capture: the grid's last row, Brno, is cut by its own screenshot; a taller capture needs the data
app unlocked, which needs its password. VERIFY(owner). */}

**Where you are:** you have heard of Keboola and have nothing set up. **When you finish:** a
scheduled pipeline and a data app, in a project you created or joined.

The sample is six café branches: five files with a summer of sales, the rosters that served it and
the weather, plus next week's forecast from a public API that needs no account. No file answers
anything alone, and the moves are the same on your own tables. Every step works toward one
question: **Which day next week will leave a café short-handed?** By the fifth page a grid answers
it, and the answer changes every morning with the forecast.

<Prereqs needs={[]}>
  <li>
    <strong>An email address</strong> to create your project on the first page, or an invite to
    your company's project.
  </li>
  <li>
    <strong>A browser.</strong> Every step also has a Prompt way and a terminal way;{' '}
    <a href="#how-every-page-works">How every page works</a> says what those need.
  </li>
</Prereqs>

{/* The box replaced a sentence on 2026-09-24 ("Everything here can be done in the browser with
nothing installed; the command-line way on each page needs kbagent. A free project is enough to
begin."), because Nikita asked for the concrete things a reader needs rather than prose about them.
VERIFY(owner): the hub makes no Free Plan promise now; the box asks only for a project. Three
things stay open, and Get a project is where they would be answered: whether the Free Plan includes data apps
(app/index.mdx), whether Kai is on the Free Plan's fixed stack, us-east4 (every live run here was
on europe-west3, and project/index.mdx says Kai "is not on every stack"), and whether 60 runtime
minutes a month survive a daily flow whose recorded phases took 121 + 41 + 282 s
(management/pay-as-you-go/). The sentence used to say "covers all of it" and linked the wizard; it
links Get a project now, so a reader meets the invite and SSO advice before a sign-up form.
Fact-checker and guide-tester, 2026-09-23. */}

## What you get

| Step | What you'll be able to do | Keboola calls it | Minutes |
|---|---|---|---|
| [Get a project](/getting-started/project/) | create or join a project, the place everything below lives in | a **project** | 5 |
| [Get your data in](/getting-started/load/) | load five files and a live API into six tables, from configurations you can re-run | a **data source connector** | 15 |
| [Transform data](/getting-started/transform/) | join five of them with SQL into one table: a café, a day, the demand to expect, the crew rostered, a flag | a **transformation** | 15 |
| [Ask a question and get an answer](/getting-started/ask/) | ask that table a question in plain words and get an answer that says why | **Kai** reading your data | 5 |
| [Put it in front of someone](/getting-started/app/) | describe an app in a paragraph and get a grid: six cafés by seven days, red where it will hurt | a **data app** | 15 |
| [Make it run every morning](/getting-started/automate/) | schedule all of it to re-run on the new forecast, and hear about it if it breaks | a **flow** | 15 |
| [Check it worked](/getting-started/check/) | read what Storage and Jobs should show, and recognise the failures that happen | — | 5 |

Read the steps in order, or land on any one of them: each says where you should be, what it needs,
and how to tell it worked. The **Next** control at the foot of every page follows this order.

## How every page works

Every page with a task on it offers that task three ways, and your choice follows you from page
to page:

- **Prompt**: paste it into **Kai**, the assistant in the project's top bar, and watch it build.
  Kai asks before it changes anything: one approval per object it creates, while questions that
  only read do not ask. An organization admin switches the feature on; see
  [Get started with Kai](/kai/getting-started/).
- **UI**: the same task click by click, for when you want to see where each setting lives.
- **CLI / API**: the same task from a terminal with [kbagent](/cli/), and the raw request beside
  it where there is no command for it.

Pick a tab once and every page after it opens on the same one:

![Choosing the CLI / API tab on one page, then opening the next page in the guide, which is already showing its CLI / API tab](/getting-started/tab-sync.gif)

Where one of the three cannot do the job, the page says so rather than showing an empty tab. A
signup form, a Google consent screen and a branch merge are all decisions a browser has to make.

Nothing here is one-way: if a step builds something other than what the page describes, say so in
the same Kai chat, or edit the configuration by hand.

## Where to start instead

This guide is the browser path. Three others start from the same place:

| Quickstart | Description |
|---|---|
| [This guide](/getting-started/project/) | Build a scheduled pipeline and a data app in the browser, prompt first. |
| [kbagent CLI](/cli/getting-started/) | The same operations from a terminal, scriptable and agent-friendly. |
| [MCP server](/ai/mcp-server/) | Connect your own AI assistant or IDE to a Keboola project. |
| [Build an app with Kai](/data-apps/getting-started/) | Go straight to a data app on data you already have. |

## Going further

Optional side trips, once the main path makes sense. None of them are needed to finish the guide:

- **[Send a table to Google Sheets](/getting-started/write/)** delivers the result to where people
  read it. It needs a Google account you can authorize.
- **[Load from Google Sheets](/getting-started/load/googlesheets/)** and
  **[from a database](/getting-started/load/database/)** load from a source that needs credentials
  rather than from a public URL.
- **[Use a workspace](/getting-started/transform/workspace/)** is where you develop and test SQL
  against a copy of your data before committing it to a transformation.
- **[Ad-hoc data analysis](/getting-started/ad-hoc/)** explores data in a Python or R notebook
  instead of building a pipeline.
- **[Development branches](/getting-started/branches/)** change a running project safely: make the
  change, review the diff, then merge.

## If you are planning a rollout, not learning the tool

This guide is for one person building one pipeline. For introducing Keboola to a team, with
project architecture, a data model, naming conventions and governance, start with
[Platform Onboarding](/overview/onboarding/) instead.
